network:
  host: 0.0.0.0
path: # path related setting for ES
  # use this path to store snapshots and backup data
  repo: /usr/share/elasticsearch/repo
http.cors.enabled: true
http.cors.allow-origin: "*"
http.cors.allow-headers: "Authorization"
